# V9. Figure and Table Verification

## Verdict
Partial. Figure 1 and Table 3 are statically traceable to the final deduplicated `resp_df`, but Table 2 is hard-coded metadata and the README expected p-values are not fully consistent with the generated outputs.

## Static Evidence
- `docs/RCT_historical_validation/VV_PLAN.md:232-251` defines V9 checks: Figure 1 must use final deduplicated user-level data, captions/prose must define responders/non-responders, Wilson 95% CIs, two-sided binomial p-values vs 50%, and generated outputs must be compared.
- `docs/RCT_historical_validation/_build_paper_figures.py:67-82` defines `best_per_user_drug()`, returning one best-ranked record per `(user, drug)` using sentiment priority and signal-strength tie-breaks.
- `docs/RCT_historical_validation/_build_paper_figures.py:120-140` computes `n`, positive responders, negative/neutral/mixed non-responders, Wilson 95% CIs, and two-sided `binomtest(..., 0.5)` p-values in `_sentiment_breakdown()`.
- `docs/RCT_historical_validation/_build_paper_figures.py:142-165` builds `resp_df` from deduplicated per-drug records; naltrexone is separately deduplicated across the two source databases before appending to the same table.
- `docs/RCT_historical_validation/_build_paper_figures.py:174-230` draws Figure 1 directly from `resp_df`, including responder/non-responder bars, Wilson CI error bars, rounded percentage labels, y-axis `n` labels, and trial-direction annotations/legend.
- `docs/RCT_historical_validation/_build_paper_figures.py:236-243` and `297-322` include prose that data are pre-publication, responders are positive, non-responders are negative + neutral + mixed, CIs are Wilson 95%, and p-values are two-sided binomial tests against 50%.
- `docs/RCT_historical_validation/_build_paper_figures.py:247-263` constructs Table 2 from literal `_src_rows`, so its database/window/post/user/report values are not recomputed from the source databases in this script.
- `docs/RCT_historical_validation/_build_paper_figures.py:297-322` builds Table 3 from `resp_df`, so Table 3 shares the same source table as Figure 1.
- `docs/RCT_historical_validation/_gen_analysis.py:47-85`, `93-131`, `137-188`, and `223-240` implement an alternate/generated notebook path with the same deduplication, `resp_df`, Figure 1 logic, Wilson CIs, and binomial p-value formatting.
- `docs/RCT_historical_validation/analysis.ipynb` output shows the same core `resp_df` values as the build output: famotidine `n=207`, `78.743961%`, `p=2.880285e-17`; loratadine `n=107`, `68.224299%`, `p=2.063747e-04`; naltrexone `n=76`, `65.789474%`, `p=0.007905`; colchicine `n=40`, `62.5%`, `p=0.153860`; paxlovid `n=153`, `52.287582%`, `p=0.627771`; prednisone `n=176`, `52.272727%`, `p=0.597872`.
- `docs/RCT_historical_validation/output/paper_figures.html:7871-7950` displays Table 3 values matching the build script and V8 expected values, including Wilson CI ranges and p-values.
- `docs/RCT_historical_validation/output/paper_figures.ipynb` and `paper_figures_executed.ipynb` contain the same `resp_df`-based Figure 1/Table 3 code as `_build_paper_figures.py`.
- Static image inspection of `docs/RCT_historical_validation/output/figure1.png` shows the expected drug order, `n` values, rounded responder/non-responder labels, Wilson CI axis label, and `trial: positive`/`trial: null` annotations.

## Gaps and Risks
- Table 2 is hard-coded in `_src_rows`; static review cannot prove `total_posts`, `unique_users`, and `treatment_reports` match the current databases without recomputing SQL counts.
- `docs/RCT_historical_validation/README.md:119-130` lists expected p-values that differ from the generated Table 3 for loratadine, naltrexone, paxlovid, and prednisone, suggesting stale README values or a changed statistical calculation.
- Figure 1 percentage labels are rounded to whole percentages, while Table 3 uses one decimal place; this is visually acceptable but should be intentional in manuscript presentation.
- `output/figure1.png` can only be visually inspected here; no OCR or pixel-level comparison was run.
- Static review did not execute notebooks or rebuild outputs, so consistency is limited to source-code inspection plus existing artifact inspection.

## Suggested Follow-up Checks
- Run `python docs/RCT_historical_validation/_build_paper_figures.py` from a clean environment with the frozen databases and confirm all generated outputs are unchanged.
- Independently recompute Table 2 `total_posts`, `unique_users`, and `treatment_reports` from SQLite and compare to `_src_rows`.
- Update or reconcile `README.md` expected p-values against the generated Table 3 and V8 expected values.
- Compare `output/figure1.png` against the regenerated figure using an image diff and manually verify final manuscript captions match the notebook prose.
